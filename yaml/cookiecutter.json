{
    "directory_name":  "",
    "project_name": "{{ cookiecutter.directory_name.lower().replace(' ', '').replace('-', '').replace('_', '') }}",
    "repository": "{{ cookiecutter.directory_name }}",
    "develop_branch": "master",
    "api_description": "API description",
    "api_host": "api.example.com",
    "environment": "dev",
    "_lambda_name": "{{cookiecutter.lambda_name}}",
    "_memory_size": "{{cookiecutter.memory_size}}",
    "_timeout": "{{cookiecutter.timeout}}"
}